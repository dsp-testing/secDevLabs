name: Flutter for Android

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  
defaults:
  run:    
    working-directory: owasp-top10-2016-mobile/m5/panda_zap/mobile

jobs:  
  build:
    runs-on: ubuntu-latest    

    steps:
      - uses: actions/checkout@v2

    # - name: Set up JDK
    #   uses: actions/setup-java@v1
    #   with:
    #     java-version: 11

    # - name: Setup Android SDK
    #   uses: android-actions/setup-android@v2

      - name: Setup Flutter SDK
        uses: flutter-actions/setup-flutter@v2
        with:
          channel: stable
          version: 3.0.2

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze project source
        run: dart analyze
        
      - name: Analyze project source
        run: flutter analyze

      #- name: Run tests
      #  run: flutter test
